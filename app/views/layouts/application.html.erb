<!DOCTYPE html>
<html>
  <head>
    <title>ChurchVotingApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <%= csrf_meta_tags %>

    <style>
      /* Style for the header with logos */
      .header {
        display: flex;
        justify-content: space-around;
        align-items: center;
        width: 100%;
        padding: 10px 20px;
        background-color: white;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
        position: fixed;
        top: 0;
        left: 0;
        z-index: 1000;
      }

      .logo {
        width: 70px;
        height: auto;
      }

      /* Add padding to the body to avoid content behind the fixed header */
      body {
        padding-top: 100px;
        font-family: Arial, sans-serif;
        background-color: #f4f4f9;
      }

      /* Style for the navigation menu */
      nav ul {
        list-style-type: none;
        display: flex;
        gap: 20px;
        margin: 0;
        padding: 0;
      }

      nav li {
        display: flex;
        align-items: center;
      }

      nav a, nav a button {
        text-decoration: none;
        color: #333;
        font-size: 16px;
        padding: 10px 15px;
        background-color: #007BFF;
        color: white;
        border-radius: 5px;
        transition: background-color 0.3s;
        border: none; /* Remove default button border */
        cursor: pointer; /* Change cursor to pointer */
      }

      nav a:hover, nav a button:hover {
        background-color: #0056b3;
      }

      /* Responsive header */
      @media (max-width: 768px) {

        nav ul {
          flex-direction: column;
          gap: 10px;
        }

        .logo {
          width: 50px;
        }
      }

      /* Notice message styling */
      p {
        background-color: #d4edda;
        color: #155724;
        padding: 15px;
        border: 1px solid #c3e6cb;
        border-radius: 5px;
        width: 80%;
        margin: 20px auto;
        text-align: center;
      }

      /* Style for button_to specifically */
      .button {
        padding: 10px 15px; /* Adjust padding if needed */
        background-color: #007BFF; /* Button background color */
        color: white; /* Button text color */
        border: none; /* Remove border */
        border-radius: 5px; /* Rounded corners */
        cursor: pointer; /* Pointer cursor on hover */
        transition: background-color 0.3s; /* Smooth background transition */
      }

      .button:hover {
        background-color: #0056b3; /* Darker blue on hover */
      }

    </style>

  </head>

  <body>
    <header class="header">
      <%= image_tag 'logowhite.jpg', alt: 'Left Logo', class: 'logo' %> <!-- Replace with your actual logo image file -->

      <%= image_tag 'logowhite.jpg', alt: 'Right Logo', class: 'logo' %>
    </header>

  <div class="container">
    <% flash.each do |key, message| %>
      <div class="flash-message <%= key %>"><%= message %></div>
    <% end %>
    <%= yield %>
    
    <% if notice %>
      <p><%= notice %></p>

      
      <% if current_user&.developer? %>
        <%= link_to 'Sign Up', new_user_registration_path %>
      <% end %>
    <% end %>

    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
  </body>
</html>
