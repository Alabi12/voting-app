<div class="container">
  <h1 class="welcome-title">P.C.G ZIMMERMANN MEMORIAL, NUMGUA</h1>
  <h2 class="welcome-title">Young People's Guild</h2>
  <h1>Welcome to the Voting Platform</h1>

  <% if current_user %>
    <% if current_user.admin? %>
      <p>As an admin, you can manage voters, add candidates, and view the summary:</p>
      <%= link_to 'Manage Voters', admin_voters_path, class: 'action-link' %>
      <%= link_to 'Add Candidate', new_admin_candidate_path, class: 'action-link' %>
      <%= link_to 'View Admin Summary', admin_votes_summary_path, class: 'action-link' %>
    <% else %>
      <p>Cast your vote here:</p>
      <%= link_to 'Vote Now', positions_path, class: 'action-link' %>
    <% end %>
  <% else %>
    <ul class="options-list">
      <li class="options-item">
        <%= link_to 'Vote for Candidates', positions_path, class: 'options-link' %>
      </li>
      <li class="options-item">
        <%= link_to 'View Voting Summary', admin_votes_summary_path, class: 'options-link' %>
      </li>
    </ul>
  <% end %>
</div>

<style>
  .container {
    text-align: center;
    padding: 20px;
  }

  .welcome-title {
    font-size: 28px;
    font-weight: bold;
    margin-bottom: 15px;
    color: #333;
  }

  .action-link, .options-link {
    display: inline-block;
    padding: 10px 20px;
    color: #fff;
    text-decoration: none;
    border-radius: 5px;
    transition: background-color 0.3s ease;
    margin: 10px 0;
  }

  .action-link {
    background-color: #28a745;
  }

  .action-link:hover {
    background-color: #218838;
  }

  .options-link {
    background-color: #007BFF;
  }

  .options-link:hover {
    background-color: #0056b3;
  }

  .options-list {
    list-style-type: none;
    padding: 0;
  }

  .options-item {
    margin: 10px 0;
  }
</style>