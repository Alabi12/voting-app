<!-- app/views/shared/_flash.html.erb -->
<% if flash.any? %>
  <div class="flash">
    <% flash.each do |key, message| %>
      <div class="<%= key %>"><%= message %></div>
    <% end %>
  </div>
<% end %>
